# Add a custom permission for an ACTION of a contract
# this is for security reason just like in play, disndcards ACTION of WAX.
# The key is used inside the FE-code which if compromised then the account is secure, as neither the active, owner is compromised.